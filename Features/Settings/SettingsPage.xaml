<Page
    x:Class="DevDeck.Features.Settings.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="32,28,32,32">
        <Grid ColumnSpacing="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="520"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="24">
                <TextBlock Text="Settings" Style="{StaticResource HeaderTextBlockStyle}" FontWeight="SemiBold"/>

                <StackPanel Spacing="12">
                    <TextBlock Text="Appearance" Style="{StaticResource BodyStrongTextBlockStyle}"/>

                    <Border BorderBrush="{ThemeResource SystemControlBackgroundListLowBrush}"
                            BorderThickness="1"
                            CornerRadius="{ThemeResource OverlayCornerRadius}"
                            Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                        <Grid Padding="16" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="240"/>
                            </Grid.ColumnDefinitions>
                            <SymbolIcon Symbol="Setting" Grid.Column="0" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                <TextBlock Text="App theme" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                <TextBlock Text="Select which app theme to display" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                            </StackPanel>
                            <ComboBox x:Name="ThemeComboBox" Grid.Column="2" HorizontalAlignment="Stretch" SelectionChanged="ThemeComboBox_SelectionChanged">
                                <ComboBoxItem Content="Light"/>
                                <ComboBoxItem Content="Dark"/>
                                <ComboBoxItem Content="Use system setting"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <Border BorderBrush="{ThemeResource SystemControlBackgroundListLowBrush}"
                            BorderThickness="1"
                            CornerRadius="{ThemeResource OverlayCornerRadius}"
                            Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                        <Grid Padding="16" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="240"/>
                            </Grid.ColumnDefinitions>
                            <SymbolIcon Symbol="View" Grid.Column="0" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                <TextBlock Text="Window backdrop" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                <TextBlock Text="Choose the base material for the main window" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                            </StackPanel>
                            <ComboBox x:Name="BackdropComboBox" Grid.Column="2" HorizontalAlignment="Stretch" SelectionChanged="BackdropComboBox_SelectionChanged">
                                <ComboBoxItem Content="Mica"/>
                                <ComboBoxItem Content="Mica Alt"/>
                                <ComboBoxItem Content="Solid background"/>
                            </ComboBox>
                        </Grid>
                    </Border>
                </StackPanel>

                <StackPanel Spacing="12">
                    <TextBlock Text="Actions" Style="{StaticResource BodyStrongTextBlockStyle}"/>

                    <Border BorderBrush="{ThemeResource SystemControlBackgroundListLowBrush}"
                            BorderThickness="1"
                            CornerRadius="{ThemeResource OverlayCornerRadius}"
                            Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid Padding="16" ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="240"/>
                                </Grid.ColumnDefinitions>
                                <SymbolIcon Symbol="OpenWith" Grid.Column="0" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                    <TextBlock Text="Default shell" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Text="Shell used when a new command action is created" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                </StackPanel>
                                <ComboBox x:Name="DefaultShellComboBox" Grid.Column="2" HorizontalAlignment="Stretch" SelectionChanged="DefaultShellComboBox_SelectionChanged">
                                    <ComboBoxItem Content="PowerShell 7"/>
                                    <ComboBoxItem Content="Windows PowerShell"/>
                                    <ComboBoxItem Content="Command Prompt (CMD)"/>
                                    <ComboBoxItem Content="Git Bash"/>
                                </ComboBox>
                            </Grid>

                            <Border Grid.Row="1" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource SystemControlBackgroundListLowBrush}">
                                <Grid Padding="16" ColumnSpacing="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="240"/>
                                    </Grid.ColumnDefinitions>
                                    <SymbolIcon Symbol="FourBars" Grid.Column="0" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                        <TextBlock Text="Project action button size" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock Text="Controls the density of action buttons inside Project pages" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                    </StackPanel>
                                    <ComboBox x:Name="ActionButtonSizeComboBox" Grid.Column="2" HorizontalAlignment="Stretch" SelectionChanged="ActionButtonSizeComboBox_SelectionChanged">
                                        <ComboBoxItem Content="Compact"/>
                                        <ComboBoxItem Content="Standard"/>
                                        <ComboBoxItem Content="Large"/>
                                    </ComboBox>
                                </Grid>
                            </Border>

                            <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource SystemControlBackgroundListLowBrush}">
                                <Grid Padding="16" ColumnSpacing="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="240"/>
                                    </Grid.ColumnDefinitions>
                                    <SymbolIcon Symbol="Play" Grid.Column="0" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                        <TextBlock Text="Maximum concurrent actions" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock Text="Limits how many actions can run at the same time" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                    </StackPanel>
                                    <NumberBox x:Name="MaxConcurrentActionsInput"
                                               Grid.Column="2"
                                               HorizontalAlignment="Stretch"
                                               Value="4"
                                               SpinButtonPlacementMode="Compact"
                                               Minimum="1"
                                               Maximum="16"
                                               ValueChanged="MaxConcurrentActionsInput_ValueChanged"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>
                </StackPanel>

                <StackPanel Spacing="12">
                    <TextBlock Text="Data management" Style="{StaticResource BodyStrongTextBlockStyle}"/>

                    <Border BorderBrush="{ThemeResource SystemControlBackgroundListLowBrush}"
                            BorderThickness="1"
                            CornerRadius="{ThemeResource OverlayCornerRadius}"
                            Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid Padding="16" ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <SymbolIcon Symbol="Folder" Grid.Column="0" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                    <TextBlock Text="Data folder" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Text="Open DevDeck's local data directory" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                </StackPanel>
                                <Button Grid.Column="2" Click="OpenDataFolder_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <SymbolIcon Symbol="OpenFile"/>
                                        <TextBlock Text="Open"/>
                                    </StackPanel>
                                </Button>
                            </Grid>

                            <Border Grid.Row="1" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource SystemControlBackgroundListLowBrush}">
                                <Grid Padding="16" ColumnSpacing="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <SymbolIcon Symbol="Upload" Grid.Column="0" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                        <TextBlock Text="Export configuration" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock Text="Save a backup JSON file for workspaces, projects, actions, and settings" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                    </StackPanel>
                                    <Button Grid.Column="2" Click="ExportConfig_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <SymbolIcon Symbol="Upload"/>
                                            <TextBlock Text="Export"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Border>

                            <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource SystemControlBackgroundListLowBrush}">
                                <Grid Padding="16" ColumnSpacing="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <SymbolIcon Symbol="Download" Grid.Column="0" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                        <TextBlock Text="Import configuration" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock Text="Restore from a DevDeck backup file" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                    </StackPanel>
                                    <Button Grid.Column="2" Click="ImportConfig_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <SymbolIcon Symbol="Download"/>
                                            <TextBlock Text="Import"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Border>

                            <Border Grid.Row="3" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource SystemControlBackgroundListLowBrush}">
                                <Grid Padding="16" ColumnSpacing="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <SymbolIcon Symbol="Delete" Grid.Column="0" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                        <TextBlock Text="Reset app data" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock Text="Remove DevDeck records without deleting real project folders" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                    </StackPanel>
                                    <Button Grid.Column="2" Click="ResetApp_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <SymbolIcon Symbol="Delete"/>
                                            <TextBlock Text="Reset"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Spacing="20" Margin="0,76,0,0">
                <StackPanel Spacing="8">
                    <TextBlock Text="About this app" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <TextBlock Text="DevDeck" Style="{StaticResource BodyTextBlockStyle}"/>
                    <TextBlock Text="Windows App SDK 2.2.0" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                    <TextBlock Text="A project launcher and shared action deck for Windows." TextWrapping="WrapWholeWords" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                </StackPanel>

                <HyperlinkButton Content="Microsoft Windows app design guidelines" NavigateUri="https://learn.microsoft.com/windows/apps/design/"/>
                <HyperlinkButton Content="Windows App SDK" NavigateUri="https://learn.microsoft.com/windows/apps/windows-app-sdk/"/>

                <Button Content="Open data folder" Click="OpenDataFolder_Click" HorizontalAlignment="Stretch"/>
                <Button Content="Help" HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
