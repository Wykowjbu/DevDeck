<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DevDeck.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevDeck"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="DevDeck">

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar Grid -->
        <Grid x:Name="AppTitleBar" Height="48" VerticalAlignment="Top" Background="Transparent" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="144" />
            </Grid.ColumnDefinitions>

            <Image Source="Assets/Square44x44Logo.targetsize-24_altform-unplated.png" Width="16" Height="16" Margin="16,0,8,0" Grid.Column="0"/>
            <TextBlock Text="DevDeck" VerticalAlignment="Center" FontWeight="SemiBold" Grid.Column="0" Margin="0,0,16,0"/>

            <TextBlock x:Name="TitleBreadcrumb"
                       Text=""
                       VerticalAlignment="Center"
                       Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"
                       TextTrimming="CharacterEllipsis"
                       Grid.Column="1"/>
        </Grid>

        <!-- Shell Page Frame Container -->
        <Frame x:Name="RootFrame" Grid.Row="1" />
    </Grid>
</Window>
