<Page
    x:Class="DevDeck.Features.Home.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24">
            <!-- Workspace Info -->
            <StackPanel Spacing="4">
                <TextBlock x:Name="WorkspaceTitle" Text="Danh sách Projects" Style="{StaticResource TitleLargeTextBlockStyle}" FontWeight="SemiBold" TextWrapping="WrapWholeWords"/>
                <TextBlock x:Name="WorkspaceSub" Text="Chọn một project bên dưới hoặc tạo mới" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
            </StackPanel>

            <!-- Projects Grid (using GridView) -->
            <GridView x:Name="ProjectsGridView" 
                      SelectionMode="None" 
                      IsItemClickEnabled="True" 
                      ItemClick="ProjectsGridView_ItemClick">
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid BorderThickness="1" BorderBrush="{ThemeResource SystemControlBackgroundListLowBrush}" CornerRadius="{ThemeResource OverlayCornerRadius}" Padding="16" Width="220" MinHeight="140" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
                            <StackPanel Spacing="12">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE8B7;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="{ThemeResource SystemControlPageTextBaseHighBrush}"/>
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis" Width="150"/>
                                </StackPanel>
                                <TextBlock Text="{Binding FolderPath}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextTrimming="CharacterEllipsis" MaxLines="2" ToolTipService.ToolTip="{Binding FolderPath}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
            
            <Button x:Name="AddProjectBtn" Content="Thêm Project Mới" Style="{StaticResource AccentButtonStyle}" Click="AddProjectBtn_Click" Margin="0,16,0,0"/>
        </StackPanel>
    </ScrollViewer>
</Page>
